spring:
  profiles:
    active: test
  application:
    name: user-service
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
    bus:
      trace:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://10.18.0.10:8080/eureka/

